---
interface Props {
  label: string;
  variant?: 'default' | 'seq';
  href?: string;
}
const { label, variant = 'default', href } = Astro.props;
const cls = variant === 'seq' ? 'chip chip-seq' : 'chip';
const Tag = href ? 'a' : 'span';
---
<Tag class={cls} href={href}>{label}</Tag>
